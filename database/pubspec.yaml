name: smartscan_database
description: Database schema for SmartScan
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

resolution: workspace

dependencies:
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  uuid: ^4.5.1
  smartscan_models:
    path: ../models
  smartscan_core_engine:
    path: ../core_engine

dev_dependencies:
  build_runner: ^2.4.11
  isar_generator: ^3.1.0+1
